{
  "name": "professional-readme-generator",
  "version": "1.0.0",
  "description": "Code refactor to finish creating a javascript application that takes input from the user through the command-line and uses it to generate a readme file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Blake Burns",
  "license": "ISC",
  "dependencies": {
    "inquirer": "8.2.4"
  }
}
